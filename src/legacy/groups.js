// src/legacy/groups.js

/* * 🚨 주의: 이 파일은 빈 껍데기(Dummy)입니다!
 * 그룹 목록 기능은 이미 React(GroupListPage.jsx)로 완전히 이사했습니다.
 * 기존 legacy 코드(init, invite, auth 등)가 에러로 뻗지 않도록 
 * 이름만 남겨둔 가짜 함수들입니다.
 */

export const renderGroupList = () => {
  console.log("React가 알아서 렌더링하므로 옛날 renderGroupList는 무시됩니다.");
};

export const refreshGroupListFromServer = async () => {
  console.log("React가 알아서 데이터를 새로고침하므로 무시됩니다.");
};

export const addStudyToListUI = () => {};
export const enterGroup = () => {};
export const deleteGroup = async () => {};